/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { UserRoleService } from "../userRole.service";
import { UserRoleCreateInput } from "./UserRoleCreateInput";
import { UserRole } from "./UserRole";
import { UserRoleFindManyArgs } from "./UserRoleFindManyArgs";
import { UserRoleWhereUniqueInput } from "./UserRoleWhereUniqueInput";
import { UserRoleUpdateInput } from "./UserRoleUpdateInput";
import { StudentDatumFindManyArgs } from "../../studentDatum/base/StudentDatumFindManyArgs";
import { StudentDatum } from "../../studentDatum/base/StudentDatum";
import { StudentDatumWhereUniqueInput } from "../../studentDatum/base/StudentDatumWhereUniqueInput";

export class UserRoleControllerBase {
  constructor(protected readonly service: UserRoleService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: UserRole })
  async createUserRole(
    @common.Body() data: UserRoleCreateInput
  ): Promise<UserRole> {
    return await this.service.createUserRole({
      data: {
        ...data,

        users: data.users
          ? {
              connect: data.users,
            }
          : undefined,
      },
      select: {
        access: true,
        defaultRole: true,
        department: true,
        hideName: true,
        id: true,
        roleId: true,

        users: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [UserRole] })
  @ApiNestedQuery(UserRoleFindManyArgs)
  async userRoles(@common.Req() request: Request): Promise<UserRole[]> {
    const args = plainToClass(UserRoleFindManyArgs, request.query);
    return this.service.userRoles({
      ...args,
      select: {
        access: true,
        defaultRole: true,
        department: true,
        hideName: true,
        id: true,
        roleId: true,

        users: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: UserRole })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async userRole(
    @common.Param() params: UserRoleWhereUniqueInput
  ): Promise<UserRole | null> {
    const result = await this.service.userRole({
      where: params,
      select: {
        access: true,
        defaultRole: true,
        department: true,
        hideName: true,
        id: true,
        roleId: true,

        users: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: UserRole })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateUserRole(
    @common.Param() params: UserRoleWhereUniqueInput,
    @common.Body() data: UserRoleUpdateInput
  ): Promise<UserRole | null> {
    try {
      return await this.service.updateUserRole({
        where: params,
        data: {
          ...data,

          users: data.users
            ? {
                connect: data.users,
              }
            : undefined,
        },
        select: {
          access: true,
          defaultRole: true,
          department: true,
          hideName: true,
          id: true,
          roleId: true,

          users: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: UserRole })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteUserRole(
    @common.Param() params: UserRoleWhereUniqueInput
  ): Promise<UserRole | null> {
    try {
      return await this.service.deleteUserRole({
        where: params,
        select: {
          access: true,
          defaultRole: true,
          department: true,
          hideName: true,
          id: true,
          roleId: true,

          users: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/studentData")
  @ApiNestedQuery(StudentDatumFindManyArgs)
  async findStudentData(
    @common.Req() request: Request,
    @common.Param() params: UserRoleWhereUniqueInput
  ): Promise<StudentDatum[]> {
    const query = plainToClass(StudentDatumFindManyArgs, request.query);
    const results = await this.service.findStudentData(params.id, {
      ...query,
      select: {
        area: true,
        citizenship: true,
        curriculum: true,
        degree: true,
        department: true,
        enterDate: true,
        gender: true,
        guid: true,
        id: true,
        institutesId: true,
        institutesName: true,
        mltSwitch: true,
        parttime: true,
        program: true,
        residency: true,
        status: true,
        statusDate: true,

        studentDataRi: {
          select: {
            id: true,
          },
        },

        undergrad: true,

        userRole: {
          select: {
            id: true,
          },
        },

        year: true,
        yearAdjust: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/studentData")
  async connectStudentData(
    @common.Param() params: UserRoleWhereUniqueInput,
    @common.Body() body: StudentDatumWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      studentData: {
        connect: body,
      },
    };
    await this.service.updateUserRole({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/studentData")
  async updateStudentData(
    @common.Param() params: UserRoleWhereUniqueInput,
    @common.Body() body: StudentDatumWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      studentData: {
        set: body,
      },
    };
    await this.service.updateUserRole({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/studentData")
  async disconnectStudentData(
    @common.Param() params: UserRoleWhereUniqueInput,
    @common.Body() body: StudentDatumWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      studentData: {
        disconnect: body,
      },
    };
    await this.service.updateUserRole({
      where: params,
      data,
      select: { id: true },
    });
  }
}
